import { WorksPage } from "@/features/works/pages/works-page";

/**
 * Our Works Route
 * 
 * Publicly accessible route at /our-works.
 * Imports the main page assembly from the works feature.
 */
export default function Page() {
    return <WorksPage />;
}
